# 未確定事項一覧 — Private salon 結 -Yui- 法人向けLP

チラシ（要件書 §3）や素材フォルダに記載のない事実は創作せず、`wireframe.md` 内で `TODO:` として
プレースホルダ化した。実装（骨組み着手）前にできるだけ回答をお願いしたい項目を、影響範囲別にまとめる。

---

## A. コンテンツとして「TODO:」表示になる項目（回答があり次第、原稿に反映）

1. **【回答済み】対応エリアの正確な範囲**
   出張費体系の回答を得たため `src/data/faq.ts` Q4を確定済み（`confirmed: true`）：
   「宮城県仙台市を中心に対応。出張費は10km未満無料、10kmを超える場合は10kmごとに1,000円」。
   市区町村単位の正確な境界線までは未確定だが、FAQ回答としては運用可能なレベルで確定した。

2. **【回答済み】電話番号**
   掲載しない方針で確定（メール・LINEのみで受付）。実装は元々電話番号欄を作っていないため
   コード変更なし。

3. **LINE友だち追加のURL**
   素材は QRコード画像（`line-qr.png`）のみで、遷移先URLが要件書・素材双方に見当たらない。
   クロージングセクションとモバイル固定CTAバーの「LINEで相談する」ボタンの `href` に必要。
   QR画像だけでもリンク化できるが、テキストリンク版のCTAボタンにはURL文字列が要る。

4. **FAQの回答内容（11問すべて）**
   要件書 §5 は質問文のみが確定情報で、回答は「未確定→TODO」と明記されている。
   特に以下は事業運営に関わるため個別確認したい：
   - Q1 施術スペース（ベッド設置可否／会議室可否）
   - Q2 1回の訪問で施術できる人数の上限
   - Q3 着替えの要否
   - Q5 対応可能な時間帯・曜日（就業時間内／休憩時間／終業後のいずれに対応するか）
   - Q6 最低利用時間・最低人数
   - Q7 キャンセル・日程変更ポリシー
   - Q8 支払い方法の詳細（支払サイト等）
   - Q9 定期訪問の契約形態（都度／月額／年間）
   - Q10 衛生管理の方針
   - Q11 効果実感までの目安（表現は「個人差があります」で締める方針は確定済み。数字を出すかどうかのみ確認）

5. **サービス資料（PDF）の有無・提供時期**
   要件書は「資料が未用意ならフェーズ2。初期は『LINEで相談する』で代替」と方針自体は確定済み。
   PDF資料を用意する予定がある場合、いつ頃切り替えるか（初期リリースからPDF版で出す可能性も含め）を
   把握しておきたい。無ければ現行方針（LINE代替）のまま進める。

---

## B. サイト全体の設定・メタ情報に関わる項目

6. **【回答済み】屋号の正式表記**
   フル表記「Private salon 結 -Yui-」で確定。`src/data/site.ts` の `businessName` に一元化済みで、
   `<title>`・JSON-LD・ヘッダーとも参照元は同一のため、実装側の追加変更なし
   （メールタイトル等、文字数制約で短縮する箇所のみ「結 -Yui-」表記を維持）。

7. **事業形態・代表者名など、構造化データ（LocalBusiness）に追加できる情報の有無**
   住所・電話は未確定のため該当プロパティごと省略する方針（要件書 §9 で明記済み・対応済み）だが、
   代表者名・設立年・SNSアカウント等、追加で載せられる公開情報があれば教えてほしい（なければ最小構成で進める）。

8. **インボイス登録番号を表示するか**
   「インボイス登録済」バッジのみか、登録番号（T+13桁）まで表示するかは要件書に指定がない。
   表示する場合は番号が必要。

9. **【回答済み】画像素材のライセンス**
   `hero-main.png` / `menu-01〜06` / `therapist.png` はすべてフリー素材またはAI生成素材であることを
   確認済み。Web掲載ライセンス上の問題なし。

10. **【対応済み】ロゴ／アイコン素材の扱い**
    アイコンPNG（`icon-*.png`）は、新規に外部ライブラリ（lucide等）を追加せず、
    `src/icons/paths.ts` に自作したインラインSVG（`--color-rose-brand` に統一した単色ライン
    アイコン）に置き換えて実装した（要件書 §2 の「依存を追加する際は承認を得ること」に配慮し、
    新規依存を増やさない方針を優先）。元のPNGアイコンは `public/images/` に残しているが未使用。

    ロゴは新マーク（手のひらとハート／グラデーション円、支給素材一式は`logo/`）に差し替え済み
    （`prompts/02-replace-logo.md`）。旧`logo-full.png`は削除。ヘッダーは`logo-mark.png`
    （44px）とテキスト併記、favicon/apple-touch-icon/PWAマニフェスト用アイコンは支給の
    書き出し済みファイル（`favicon-simple-*` / `logo-mark-*` / `logo-mark-maskable-512`）を
    そのまま`public/`直下に配置して参照する方式に変更した（以前の`astro:assets`での動的
    リサイズ生成はやめ、小サイズ専用の簡略版と通常版を要件通り使い分け）。

    OGP画像（`public/ogp.png`）は要件通り新規作成（背景白・左にロゴ+屋号・中央〜右に
    メインコピー・下部に小さく資格/対応/エリア）。ただし本サンドボックスにはフォント
    レンダリング環境（fontconfig）が無く`sharp`/ImageMagickでの日本語テキスト焼き込みが
    できなかったため、Satori（Vercel製、フォントデータを直接渡してシステムフォント非依存で
    SVGレイアウトするライブラリ）+ `@resvg/resvg-js`で生成した。どちらも本番の依存には
    加えておらず、生成用に一時ディレクトリへインストールして使い捨てたのみ
    （`package.json`は無変更）。**表示上の見た目に問題がないか、事業者側で最終確認を推奨**
    （本サンドボックスは実ブラウザでの目視確認ができないため、生成画像のPNGを直接確認した
    のみで、Slack/LINE等の実際のOGP展開表示は未検証）。

11. **正式ドメイン／公開URL**
    `canonical` タグ、OGP `og:url`、構造化データの `url` プロパティに必要。Cloudflare Pagesのプロジェクト名や
    独自ドメインの割り当て予定があれば教えてほしい（未定の場合はデプロイ先が決まるまでプレースホルダで進める）。

12. **【ドラフト対応済み・要レビュー】プライバシーポリシーの有無**
    既存の文書が確認できなかったため、`src/pages/privacy.astro`（`/privacy/`）に最低限の
    ドラフトを作成した（取得する情報の範囲＝メール・LINEで任意にいただいた連絡先のみ、
    Cookie/アクセス解析は使用していない旨、開示・削除請求の連絡先）。既存の正式な文書があれば
    差し替え、なければ**公開前に事業者側・必要に応じて専門家の確認を受けること**（§Cの表現の
    最終確認と合わせて）。

---

## C. リーガル・表現面（要件書 §8 に基づく再確認）

13. **表現の最終確認**
    要件書 §8 の禁止表現（「治る」「治療」「改善します」等の断定表現の排除、根拠のない優位性表現の禁止）に
    沿って原稿を作成するが、これは法的助言ではない。**公開前に事業者側で景品表示法・医療広告関連の表現を
    最終確認していただく**ことを README にも明記する。

---

## D. 決定事項（要件書からの追加・変更）

14. **予約サイト（tomocha）へのリンクを追加する**
    要件書には無かった要素として、フッターに「既にご契約中の企業様はこちら」リンクを追加し、
    tomochaの予約ページ（`/reserve?source=yui-corp`）へ遷移できるようにする方針が決定した。
    主要2CTA（見積り依頼・LINE相談）とは別枠の副次的な導線として扱う（詳細は `wireframe.md` 「予約サイト連携」参照）。
    - LP側の実装コストはURL文字列を1つ持つだけで小さい。
    - `?source=yui-corp` を受け取ってtomocha側で記録する実装は、tomocha側リポジトリの別対応となる
      （具体案：`yui_reserve/docs/yui-lp-integration.md`（別リポジトリ））。この対応が完了する前でもLP側のリンクは
      先に設置してよい（パラメータが無視されるだけで予約自体は現状どおり機能する）。
    - **要確認**：tomochaの本番URL（ドメイン）が未定なので、決まり次第 `src/data/site.ts` に反映する。

---

## 現時点での対応方針（回答が揃うまでの暫定運用）

- 上記の未確定事項は、実装段階では該当箇所に `TODO:` コメント／プレースホルダテキストを表示し、
  ビルドは通る状態を維持する（空文字やダミーの電話番号・住所は入れない）。
- 構造化データ（JSON-LD）は、住所・電話など未確定のプロパティを丸ごと省略した最小構成（`name` / `description` /
  `image` / `url`(仮) 程度）でまず実装し、確定次第プロパティを追加する。
- 画像ライセンスが未確認の間も、差し替えが1箇所で完結する構成（`src/data/` からのファイル名参照）で
  実装するため、後からの差し替えに実装上の手戻りは発生しない。

ご回答いただき次第、`wireframe.md` の該当箇所を確定文言に更新し、実装（骨組み→スタイル→モーション）に進みます。
